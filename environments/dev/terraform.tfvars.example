# copy to terraform.tfvars and fill in real values (gitignored)

project        = "opella"
environment    = "dev"
location       = "eastus"
location_short = "eus"

allowed_ssh_cidr = "203.0.113.10/32" # your IP, not this one

owner       = "platform-team"
cost_center = "eng-devops"

# ssh_public_key = "ssh-rsa AAAA..." # optional, else terraform generates one
